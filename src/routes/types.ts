export type RootStackParamList = {
  Home: {
    id: string;
  };
  CarDetails: {
    id: string;
  };
  Scheduling: {
    id: string;
  };
  SchedulingDetails: {
    id: string;
  };
  SchedulingComplete: {
    id: string;
  };
};
